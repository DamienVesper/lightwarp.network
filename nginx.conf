server {
    listen 80;

    server_name lihtwarp.network;
    rewrite ^/$ https://lihtwarp.network redirect;
}

server {
    listen 443 ssl;

    server_name lihtwarp.network;

    location / {
        proxy_set_header X-Forwarded-For $remote_addr;
        proxy_pass http://127.0.0.1:8950;
    }

    ssl_certificate /etc/letsencrypt/live/lihtwarp.network/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/lihtwarp.network/privkey.pem;
}